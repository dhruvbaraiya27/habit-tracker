version: '3.8'

services:
  app:
    build: .
    container_name: habit-tracker-app
    ports:
      - "8080:8080"
    volumes:
      - habit-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080

volumes:
  habit-data: